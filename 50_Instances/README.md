# 50_Instances

Task-derived memory instances.

Instances are smaller than Wiki notes. They capture what happened in a task and may later become promotion candidates.

Subfolders:

- `accepted`: reviewed and accepted instances
- `candidates`: pending promotion candidates

Raw instances are local-only by default and are ignored by `.gitignore`.

