# .dino/context-packs

Context Pack trace records created by the DinoBrain MCP `get_context_pack` tool.

These records explain:

- the user question
- the ranking inputs used
- which records were scanned
- which items were included
- why each item was included
- how many records were excluded

Context Pack traces are conditional sync data because they may include user questions and selected memory summaries.

